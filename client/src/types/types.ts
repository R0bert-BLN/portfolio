export interface Tokens {
    accessToken: string
    refreshToken: string
}

export interface Profile {
    first_name: string
    last_name: string
    job_title: string
    description: string
    cv_url: string
    picture_url: string
    github_link: string
    linkedin_link: string
}
